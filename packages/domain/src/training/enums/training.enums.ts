export enum TrainingFormat {
  GROUP = 'GROUP',
  INDIVIDUAL = 'INDIVIDUAL',
}

export enum TrainingStatus {
  AVAILABLE = 'available',
  WAITING = 'waiting',
  FULL = 'full',
}

export enum TrainingColor {
  GOLD = 'gold',
  LIME = 'lime',
  GEEKBLUE = 'geekblue',
  YELLOW = 'yellow',
  GREEN = 'green',
}
